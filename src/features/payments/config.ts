export const CASH_QUICK_AMOUNTS = [50, 100, 500, 1000] as const

/** Path to a static merchant QR image (e.g. '/gcash-qr.png'). Null shows a placeholder box instead. */
export const GCASH_QR_IMAGE_URL: string | null = null